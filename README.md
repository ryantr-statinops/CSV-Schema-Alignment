# CSV Schema Alignment

Extensible schema matching engine for aligning semantically equivalent columns across CSV datasets.

## Goals

- Header normalization
- Text similarity
- Value-based similarity
- Data profiling
- Embedding similarity
- Score fusion
- Hungarian assignment

## Development

```bash
python -m schema_alignment.cli --help
```

